---
description: never start a benchmark while another terminal is running a benchmark. While for other terminal's benchmarks to be over
# applyTo: 'never start a benchmark while another terminal is running a benchmark. While for other terminal's benchmarks to be over' # when provided, instructions will automatically be added to the request context when the pattern matches an attached file
---
Provide project context and coding guidelines that AI should follow when generating code, answering questions, or reviewing changes.